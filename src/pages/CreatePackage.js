import React from 'react';
import AdminForm from '../components/AdminForm/AdminForm';

/**
 * 
 * @param {CreatePackage} props - page to create package
 */
const CreatePackage = (props) => {
  return (
    <AdminForm />
  )
}

export default CreatePackage;